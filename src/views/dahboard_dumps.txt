  :class="['row ', id % 2 === 0 ? 'scorecard-selected' : '']"


   fetchTopAnnouncements(){
      const self = this;
      let series  = []
      // for (let i = 0; i < self.Dashboard.dailySessions.length; i++) {
      self.Dashboard.topAnnoucement.forEach(sessions => {

       series.push(sessions)
      });
      return series;
    }
